<h1>TASK DETAIL</h1>

<h2> <%= @task.title %> </h2>
<h3> <%= @task.details %> </h3>

<%= link_to "Edit your task", edit_task_path %>

<%= link_to "Delete #{@task.title}", task_path(@task), method: :delete, data: { confirm: "Are you sure?" } %> <i class="fas fa-trash"></i>

